<!-- Begin Page Content -->
<div class="container-fluid">
  <div class="d-sm-flex align-items-center justify-content-between mb-4">
    <h1 class="h3 mb-0 text-gray-800">Detalle de Venta</h1>
    <%= link_to admin_sales_path, class: "btn btn-sm btn-primary shadow-sm" do,  data: { turbo: false } %>
      <i class="fas fa-arrow-left fa-sm text-white-50"></i> Volver a ventas
    <% end %>
  </div>

  <% 
    # Obtenemos el movimiento de caja asociado a esta venta si existe
    movimiento = @sale.head_movimiento_caja || HeadMovimientoCaja.where(origen_id: @sale.id).first
  %>

  <div class="row">
    <div class="col-12">
      <div class="card shadow mb-4">
        <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
          <h6 class="m-0 font-weight-bold text-primary">Información General de la Venta</h6>
          <span class="badge badge-success"><%= @sale.code %></span>
        </div>
        <div class="card-body">
          <% if flash[:notice] %>
            <div class="alert alert-success"><%= flash[:notice] %></div>
          <% elsif flash[:alert] %>
            <div class="alert alert-danger"><%= flash[:alert] %></div>
          <% end %>

          <div class="row">
            <!-- Cliente -->
            <div class="col-sm-4 mb-3">
              <label><strong>Cliente</strong></label>
              <input type="text" class="form-control" value="<%= movimiento&.client_name || @sale.client_name %>" readonly>
            </div>

            <!-- Tipo de Comprobante -->
            <div class="col-sm-4 mb-3">
              <label><strong>Tipo de Comprobante</strong></label>
              <input type="text" class="form-control" value="<%= movimiento&.tipo_documento_dte&.name || @sale.tipo_documento_dte&.name || 'Venta Interna' %>" readonly>
            </div>

            <!-- Número de Control DTE / Recibo -->
            <div class="col-sm-4 mb-3">
              <label><strong>Número de Control DTE / Documento</strong></label>
              <input type="text" class="form-control font-weight-bold" value="<%= movimiento&.numero_control || movimiento&.comprobante_codigo || @sale.code %>" readonly>
            </div>

            <!-- Sucursal -->
            <div class="col-sm-4 mb-3">
              <label><strong>Sucursal</strong></label>
              <input type="text" class="form-control" value="<%= movimiento&.sucursal&.name || @sale.sucursal&.name || 'Matriz' %>" readonly>
            </div>

            <!-- Cajero / Caja -->
            <div class="col-sm-4 mb-3">
              <label><strong>Cajero / Caja</strong></label>
              <input type="text" class="form-control" value="<%= "#{movimiento&.cajero&.nombre || @sale.cajero&.nombre} (#{movimiento&.caja&.nombre || @sale.caja&.nombre})" %>" readonly>
            </div>

            <!-- Código de Generación (UUID del DTE) -->
            <div class="col-sm-4 mb-3">
              <label><strong>Código de Generación DTE (UUID)</strong></label>
              <input type="text" class="form-control small text-muted" value="<%= movimiento&.codigo_generacion || 'N/A' %>" readonly>
            </div>
          </div>

          <!-- Tabla de productos -->
          <h5 class="mb-3 mt-4">Productos Vendidos</h5>
          <div class="table-responsive">
            <table class="table table-bordered">
              <thead class="thead-light">
                <tr>
                  <th>Producto</th>
                  <th>Cantidad</th>
                  <th>Precio Unitario</th>
                  <th>Oferta</th>
                  <th>Descuento</th>
                  <th>Subtotal</th>
                </tr>
              </thead>
              <tbody>
                <% @product_sales.each do |ps| %>
                  <% 
                    discount_decimal = (ps.unit_price * ps.quantity) * ((ps.discount || 0) / 100.0)
                    subtotal = (ps.unit_price * ps.quantity) - discount_decimal 
                  %>
                  <tr>
                    <td><%= ps.product&.name || ps.product_name %></td>
                    <td><%= ps.quantity %></td>
                    <td>$<%= number_with_precision(ps.unit_price, precision: 2) %></td>
                    <td>
                      <% if ps.offer_type.present? %>
                        <span class="badge badge-info"><%= ps.offer_type %></span>
                      <% else %>
                        <span class="badge badge-secondary">N/A</span>
                      <% end %>
                    </td>
                    <td>$<%= number_with_precision(ps.discount || 0, precision: 2) %></td>
                    <td>$<%= number_with_precision(subtotal, precision: 2) %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>

          <div class="text-right mt-3">
            <h4>Total de la venta: $<%= number_with_precision(@sale.total_amount, precision: 2) %></h4>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>