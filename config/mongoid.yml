development:
  clients:
    default:
      uri: <%= ENV['MONGO_DB_URL'] %>
      options:
        log_level: :warn

test:
  clients:
    default:
      uri: <%= ENV['MONGO_DB_URL'] %>
      options:
        read:
          mode: :primary
        max_pool_size: 1
        log_level: :warn
